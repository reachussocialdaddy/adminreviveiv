const CONFIG = {
    // API_URL: 'http://localhost:10000', // For local testing
    API_URL: 'https://reviveiv-backend.onrender.com', // Update this after deploying to Render
};

window.REVIVE_CONFIG = CONFIG;
